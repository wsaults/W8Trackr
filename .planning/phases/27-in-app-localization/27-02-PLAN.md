---
phase: 27-in-app-localization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - W8TrackrWidget/Localizable.xcstrings
autonomous: true

must_haves:
  truths:
    - "Widget displays in Chinese (Simplified) when device language is zh-Hans"
    - "Widget displays in French when device language is fr"
    - "Widget displays in German when device language is de"
    - "Widget displays in Japanese when device language is ja"
    - "Widget displays in Portuguese when device language is pt-BR"
    - "Widget displays in Italian when device language is it"
    - "Widget displays in Korean when device language is ko"
    - "Widget displays in Russian when device language is ru"
  artifacts:
    - path: "W8TrackrWidget/Localizable.xcstrings"
      provides: "All widget string translations for 8 languages"
      contains: '"zh-Hans"'
  key_links:
    - from: "Widget views"
      to: "W8TrackrWidget/Localizable.xcstrings"
      via: "String(localized:) and Text()"
      pattern: 'String\\(localized:'
---

<objective>
Add translations for all 8 target languages to the widget String Catalog.

Purpose: Enable widget display in Chinese, French, German, Japanese, Portuguese, Italian, Korean, and Russian locales (ZH-02, FR-02, DE-02, JA-02, PT-02, IT-02, KO-02, RU-02)
Output: Updated W8TrackrWidget/Localizable.xcstrings with widget strings in all 8 languages
</objective>

<execution_context>
@~/.claude/agents/gsd-executor.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-localization/25-02-SUMMARY.md
@W8TrackrWidget/Localizable.xcstrings
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 8 language translations to Widget Localizable.xcstrings</name>
  <files>W8TrackrWidget/Localizable.xcstrings</files>
  <action>
Add translations for all 8 languages to every string in W8TrackrWidget/Localizable.xcstrings.

**Existing widget strings to translate (from Phase 25):**
- "Add more weigh-ins to see your trend" (empty chart prompt)
- "Add your first weigh-in" (empty state)
- "Down" / "Up" / "Same" (trend direction)
- "Goal" / "to go" / "remaining" (goal progress)
- "Weight Tracker" (widget name)
- "Track your weight progress" (widget description)
- Configuration display strings

**Pattern to follow:**
For each string key, add 8 new language localizations:

```json
"es" : { ... },  // existing
"zh-Hans" : {
  "stringUnit" : {
    "state" : "translated",
    "value" : "[Chinese translation]"
  }
},
"fr" : { ... },
"de" : { ... },
"ja" : { ... },
"pt-BR" : { ... },
"it" : { ... },
"ko" : { ... },
"ru" : { ... }
```

**Translation Guidelines:**
- Keep translations concise - widgets have limited space
- Preserve brand name "W8Trackr" exactly
- Keep weight units (lb, kg) as-is
- Preserve format specifiers (%@, %.1f, etc.)

**Widget-specific considerations:**
- Short text preferred due to widget space constraints
- "Down" should be translated to indicate weight loss (positive)
- "Up" should indicate weight gain
- "Goal" context is weight goal

Read the existing file structure first, then add all translations.
  </action>
  <verify>
1. Verify file is valid JSON: `python3 -m json.tool W8TrackrWidget/Localizable.xcstrings > /dev/null && echo "Valid JSON"`
2. Verify all 8 languages present: `grep -c '"zh-Hans"' W8TrackrWidget/Localizable.xcstrings && grep -c '"ru"' W8TrackrWidget/Localizable.xcstrings`
3. Build widget target: `xcodebuild -project W8Trackr.xcodeproj -scheme W8TrackrWidgetExtension -configuration Debug -sdk iphonesimulator build 2>&1 | tail -5`
  </verify>
  <done>
- W8TrackrWidget/Localizable.xcstrings contains translations for zh-Hans, fr, de, ja, pt-BR, it, ko, ru
- All widget strings have translations in all 8 languages
- Widget target builds without errors
  </done>
</task>

</tasks>

<verification>
1. Widget builds: `xcodebuild -project W8Trackr.xcodeproj -scheme W8TrackrWidgetExtension -configuration Debug -sdk iphonesimulator build`
2. All 8 languages present in xcstrings file
3. Valid JSON structure
</verification>

<success_criteria>
- All widget strings translated to 8 new languages
- Widget target builds successfully
- Satisfies requirements: ZH-02, FR-02, DE-02, JA-02, PT-02, IT-02, KO-02, RU-02
</success_criteria>

<output>
After completion, create `.planning/phases/27-in-app-localization/27-02-SUMMARY.md`
</output>
